/**
 * Components - Utils - Diplay - Props
 */

// React
import { bool } from 'prop-types'

const DISPLAY_PROP_TYPES = {
  dBlock: bool,
  dFlex: bool,
  dInline: bool,
  dInlineBlock: bool,
  dInlineFlex: bool,
  dNone: bool
}

export default DISPLAY_PROP_TYPES
