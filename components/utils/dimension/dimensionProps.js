/**
 * Components - Theme - Utils - Dimension
 */

// React
import { bool } from 'prop-types'

const DIMENSION_PROP_TYPES = {
  h100: bool,
  w100: bool,
  h100vh: bool,
  w100vw: bool
}

export default DIMENSION_PROP_TYPES
