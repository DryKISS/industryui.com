/**
 * Card - Props
 */

// UI
import THEME_CONTEXT from '../../../../constants/context'

const CardDefaultProps = {
  bordered: false,
  context: 'white',
  footerContext: THEME_CONTEXT.LIGHT,
  headerContext: THEME_CONTEXT.DARK,
  role: 'article',
  rounded: true,
  shadow: false,
  titleNoWrap: false
}

export default CardDefaultProps
