/**
 * Domains
 */

// React
import React, { Component, Fragment } from 'react'

// Style
import { withTheme } from 'styled-components'

export const Domains = withTheme(
  class Domains extends Component {
    render () {
      return (
        <Fragment>
          Domains
        </Fragment>
      )
    }
  }
)
